## 1. Remove Show Display Button

- [x] 1.1 Delete the `Button(onClick = controller::showDisplayWindow) { Text("Show Display") }` element from the `Row` in `HostWindow.kt`
- [x] 1.2 Verify the host window compiles and the Session card renders correctly with only "Load Pack" and "Create Game" buttons
